# Ansible Vault Encrypted Variables
# This file will be encrypted with ansible-vault
#
# To edit: ansible-vault edit inventory/group_vars/vault.yml
# To view: ansible-vault view inventory/group_vars/vault.yml

# Server credentials (will be encrypted)
vault_vps_host: "REPLACE_WITH_SERVER_IP"
vault_vps_root_password: "REPLACE_WITH_ROOT_PASSWORD"
vault_vps_user_password: "REPLACE_WITH_USER_PASSWORD"
vault_vps_username: "apexdev"
