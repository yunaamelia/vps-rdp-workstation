---
# VPS RDP Workstation Inventory
# Target: localhost for self-configuration

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

  children:
    vps:
      hosts:
        localhost:
      vars:
        # Inherit from group_vars/all.yml
