# VPS RDP Workstation - Inventory
# Supports localhost deployment
---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
  
  children:
    vps_servers:
      hosts:
        localhost:
      vars:
        # Override these for remote deployment
        # ansible_host: your-vps-ip
        # ansible_user: root
        # ansible_ssh_private_key_file: ~/.ssh/id_rsa
