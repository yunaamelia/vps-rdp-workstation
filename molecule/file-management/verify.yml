---
- name: Verify
  hosts: all
  tasks:
    - name: "Verify service file-management"
      include_tasks: ../helpers/service_verify.yml
      vars:
        service_name: "file-management"
        # expected_port: 80 # Uncomment and set if applicable
