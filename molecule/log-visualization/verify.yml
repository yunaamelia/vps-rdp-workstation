---
- name: Verify
  hosts: all
  tasks:
    - name: "Verify service log-visualization"
      include_tasks: ../helpers/service_verify.yml
      vars:
        service_name: "log-visualization"
        # expected_port: 80 # Uncomment and set if applicable
