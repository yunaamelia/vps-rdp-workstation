---
- name: Converge
  hosts: all
  vars_files:
    - ../fixtures/test_data.yml
  vars:
    vps_username: root
  tasks:
    - name: "Include role productivity"
      include_role:
        name: productivity
