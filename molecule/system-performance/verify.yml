---
- name: Verify
  hosts: all
  tasks:
    - name: "Verify service system-performance"
      include_tasks: ../helpers/service_verify.yml
      vars:
        service_name: "system-performance"
        # expected_port: 80 # Uncomment and set if applicable
