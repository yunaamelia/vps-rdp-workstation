---
- name: Converge
  hosts: all
  vars_files:
    - ../fixtures/test_data.yml
  vars:
    vps_username: root
  tasks:
    - name: "Include role text-processing"
      include_role:
        name: text-processing
