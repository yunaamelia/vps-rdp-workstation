---
- name: Verify
  hosts: all
  tasks:
    - name: "Verify service text-processing"
      include_tasks: ../helpers/service_verify.yml
      vars:
        service_name: "text-processing"
        # expected_port: 80 # Uncomment and set if applicable
