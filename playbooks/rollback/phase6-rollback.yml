---
# Phase 6 rollback - Remove validation artifacts
- name: Remove phase 6 validation logs
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ vps_setup_log_dir }}/phase6-validation-report.txt"
    - "{{ vps_setup_log_dir }}/phase6-complete.txt"
