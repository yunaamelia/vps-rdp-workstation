---
# Phase 8 rollback - Remove final validation artifacts
- name: Remove phase 8 validation logs
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ vps_setup_log_dir }}/final-validation-report.txt"
    - "{{ vps_setup_log_dir }}/phase8-complete.txt"
