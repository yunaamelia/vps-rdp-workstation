# Fonts Role - Developer Fonts Installation
# JetBrains Mono Nerd Font and Powerline fonts for Agnoster theme
---
- name: Create fonts directory
  ansible.builtin.file:
    path: /usr/share/fonts/truetype/jetbrains-mono-nerd
    state: directory
    mode: '0755'
  tags: [fonts]

- name: Create Hack Nerd Font directory
  ansible.builtin.file:
    path: /usr/share/fonts/truetype/hack-nerd
    state: directory
    mode: '0755'
  tags: [fonts]

- name: Create Fira Code Nerd Font directory
  ansible.builtin.file:
    path: /usr/share/fonts/truetype/firacode-nerd
    state: directory
    mode: '0755'
  tags: [fonts]

- name: Create Source Code Pro Nerd Font directory
  ansible.builtin.file:
    path: /usr/share/fonts/truetype/source-code-pro-nerd
    state: directory
    mode: '0755'
  tags: [fonts]

- name: Create powerline fonts directory
  ansible.builtin.file:
    path: /usr/share/fonts/truetype/powerline
    state: directory
    mode: '0755'
  tags: [fonts]

- name: Download JetBrains Mono Nerd Font
  ansible.builtin.get_url:
    url: https://github.com/ryanoasis/nerd-fonts/releases/latest/download/JetBrainsMono.tar.xz
    dest: /tmp/JetBrainsMono.tar.xz
    mode: '0644'
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Download Hack Nerd Font
  ansible.builtin.get_url:
    url: https://github.com/ryanoasis/nerd-fonts/releases/latest/download/Hack.tar.xz
    dest: /tmp/Hack.tar.xz
    mode: '0644'
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Download Fira Code Nerd Font
  ansible.builtin.get_url:
    url: https://github.com/ryanoasis/nerd-fonts/releases/latest/download/FiraCode.tar.xz
    dest: /tmp/FiraCode.tar.xz
    mode: '0644'
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Download Source Code Pro Nerd Font
  ansible.builtin.get_url:
    url: https://github.com/ryanoasis/nerd-fonts/releases/latest/download/SourceCodePro.tar.xz
    dest: /tmp/SourceCodePro.tar.xz
    mode: '0644'
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Extract JetBrains Mono Nerd Font
  ansible.builtin.unarchive:
    src: /tmp/JetBrainsMono.tar.xz
    dest: /usr/share/fonts/truetype/jetbrains-mono-nerd
    remote_src: true
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Extract Hack Nerd Font
  ansible.builtin.unarchive:
    src: /tmp/Hack.tar.xz
    dest: /usr/share/fonts/truetype/hack-nerd
    remote_src: true
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Extract Fira Code Nerd Font
  ansible.builtin.unarchive:
    src: /tmp/FiraCode.tar.xz
    dest: /usr/share/fonts/truetype/firacode-nerd
    remote_src: true
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Extract Source Code Pro Nerd Font
  ansible.builtin.unarchive:
    src: /tmp/SourceCodePro.tar.xz
    dest: /usr/share/fonts/truetype/source-code-pro-nerd
    remote_src: true
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Cleanup stale Powerline clone (if corrupted)
  ansible.builtin.file:
    path: /tmp/powerline-fonts
    state: absent
  when: vps_fonts_install_powerline_fonts and not ansible_check_mode
  tags: [fonts]

- name: Clone Powerline fonts repository
  ansible.builtin.git:
    repo: https://github.com/powerline/fonts.git
    dest: /tmp/powerline-fonts
    depth: 1
    version: master
  when: vps_fonts_install_powerline_fonts and not ansible_check_mode
  tags: [fonts]

- name: Ensure target user's local bin directory exists (for pipx/fonts)
  ansible.builtin.file:
    path: "/home/{{ vps_username }}/.local/share/fonts"
    state: directory
    owner: "{{ vps_username }}"
    group: "{{ vps_username }}"
    mode: '0755'
  tags: [fonts]

- name: Install Powerline fonts
  ansible.builtin.shell: |
    cd /tmp/powerline-fonts
    bash install.sh
  become: true
  become_user: "{{ vps_username }}"
  args:
    creates: "/home/{{ vps_username }}/.local/share/fonts/PowerlineSymbols.otf"
  when: vps_fonts_install_powerline_fonts and not ansible_check_mode
  tags: [fonts]

- name: Ensure local fontconfig directory exists
  ansible.builtin.file:
    path: "/home/{{ vps_username }}/.config/fontconfig/conf.d"
    state: directory
    owner: "{{ vps_username }}"
    group: "{{ vps_username }}"
    mode: '0755'
  tags: [fonts]

- name: Configure improved font rendering for RDP
  ansible.builtin.copy:
    dest: "/home/{{ vps_username }}/.config/fontconfig/conf.d/10-rendering.conf"
    owner: "{{ vps_username }}"
    group: "{{ vps_username }}"
    mode: '0644'
    content: |
      <?xml version='1.0'?>
      <!DOCTYPE fontconfig SYSTEM 'fonts.dtd'>
      <fontconfig>
        <match target="font">
          <edit mode="assign" name="antialias">
            <bool>true</bool>
          </edit>
          <edit mode="assign" name="hinting">
            <bool>true</bool>
          </edit>
          <edit mode="assign" name="hintstyle">
            <const>hintslight</const>
          </edit>
          <edit mode="assign" name="rgba">
            <const>rgb</const>
          </edit>
          <edit mode="assign" name="lcdfilter">
            <const>lcddefault</const>
          </edit>
        </match>
      </fontconfig>
  tags: [fonts]

- name: Configure font priority (JetBrains > Hack > FiraCode)
  ansible.builtin.template:
    src: 01-font-priority.conf.j2
    dest: "/home/{{ vps_username }}/.config/fontconfig/conf.d/01-font-priority.conf"
    owner: "{{ vps_username }}"
    group: "{{ vps_username }}"
    mode: '0644'
  tags: [fonts]

- name: Update font cache
  ansible.builtin.command: fc-cache -fv
  changed_when: false
  when: (vps_fonts_install_nerd_fonts or vps_fonts_install_powerline_fonts) and not ansible_check_mode
  tags: [fonts]

- name: Clean up downloaded archives
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - /tmp/JetBrainsMono.tar.xz
    - /tmp/Hack.tar.xz
    - /tmp/Hack.tar.xz
    - /tmp/FiraCode.tar.xz
    - /tmp/SourceCodePro.tar.xz
    - /tmp/powerline-fonts
  when: not ansible_check_mode
  tags: [fonts]

- name: Verify fonts installed
  ansible.builtin.shell: fc-list | grep -i "JetBrains"
  register: font_check
  changed_when: false
  failed_when: false
  when: vps_fonts_install_nerd_fonts and not ansible_check_mode
  tags: [fonts]

- name: Log fonts role completion
  ansible.builtin.debug:
    msg: "✓ Fonts role completed - JetBrains, FiraCode, SourceCodePro, and Powerline installed"
  tags: [fonts]
