# Productivity Role
---
- name: Ensure setuptools and wheel are installed in pipx shared environment
  ansible.builtin.command:
    cmd: /home/{{ vps_username }}/.local/share/pipx/shared/bin/pip install setuptools wheel
  become: true
  become_user: "{{ vps_username }}"
  changed_when: false
  tags: [tools, productivity]

- name: Install thefuck via pipx (Python 3.12+ compatible fork)
  community.general.pipx:
    name: thefuck
    source: "git+https://github.com/vo0ov/thefuck.git@master"
    state: install
    executable: /usr/bin/pipx
  when: install_productivity | default(true)
  tags: [tools, productivity]

- name: Patch thefuck for Python 3.12+ (replace distutils with shutil)
  vars:
    thefuck_site_packages: "/home/{{ vps_username }}/.local/share/pipx/venvs/thefuck/lib/python{{ ansible_python.version.major }}.{{ ansible_python.version.minor }}/site-packages"
  ansible.builtin.replace:
    path: "{{ thefuck_site_packages }}/thefuck/system/unix.py"
    regexp: '^from distutils\.spawn import find_executable$'
    replace: 'import shutil'
  become: true
  tags: [tools, productivity]

- name: Patch thefuck find_executable to shutil.which
  vars:
    thefuck_site_packages: "/home/{{ vps_username }}/.local/share/pipx/venvs/thefuck/lib/python{{ ansible_python.version.major }}.{{ ansible_python.version.minor }}/site-packages"
  ansible.builtin.replace:
    path: "{{ thefuck_site_packages }}/thefuck/system/unix.py"
    regexp: 'find_executable\('
    replace: 'shutil.which('
  become: true
  tags: [tools, productivity]

- name: Install tldr via pipx
  ansible.builtin.shell: |
    sudo -u {{ vps_username }} pipx install tldr
  args:
    creates: "/home/{{ vps_username }}/.local/bin/tldr"
  become: true
  tags: [tools, productivity]

- name: Add thefuck to zshrc
  ansible.builtin.blockinfile:
    path: "/home/{{ vps_username }}/.zshrc"
    marker: "# {mark} ANSIBLE MANAGED BLOCK - THEFUCK"
    block: |
      eval $(thefuck --alias)
    owner: "{{ vps_username }}"
    group: "{{ vps_username }}"
    create: true
    mode: '0644'
  tags: [tools, productivity]

- name: Log productivity role completion
  ansible.builtin.debug:
    msg: "✓ Productivity role completed"
  tags: [tools, productivity]
